import { Options } from 'prettier'

const prettierConfig: Options = {
  arrowParens: 'always',
  printWidth: 100,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
}

export = prettierConfig
